一.概 念 :
        
        过滤器.将结果为 True 的元素 返回到一个新的迭代器.

            python3返回一个迭代器,python2返回列表.

二. 格 式 :

        filter(函数,可迭代对象)

            return 迭代器

        list(迭代器)

        得到一个存有 过滤完的列表.

三. 示 例 :

    tmplist = ['春天','abc','555']

    def is_digit(n):
        return n.isdigit()

    res = filter(is_digit,tmplist)

    list(res)

